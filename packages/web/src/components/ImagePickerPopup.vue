// DESIGN SPEC — generated by Designer agent // Coder: implement TODO items, do not change component structure or accessibility attributes
<template>
  <q-menu
    class="upload-menu-popup"
    anchor="top middle"
    self="bottom middle"
    :offset="[0, 16]"
    transition-show="jump-up"
    transition-hide="jump-down"
  >
    <div class="upload-menu-popup__container">
      <button type="button" class="upload-menu-popup__item" @click="handleSelect('image')">
        <q-icon name="image" size="22px" class="upload-menu-popup__icon" />
        <span class="upload-menu-popup__label">{{ t('gallery.typeImage', '图片') }}</span>
      </button>
      
      <button type="button" class="upload-menu-popup__item" @click="handleSelect('audio')">
        <q-icon name="graphic_eq" size="22px" class="upload-menu-popup__icon" />
        <span class="upload-menu-popup__label">{{ t('gallery.typeAudio', '音频') }}</span>
      </button>

      <button type="button" class="upload-menu-popup__item" @click="handleSelect('video')">
        <q-icon name="videocam" size="22px" class="upload-menu-popup__icon" />
        <span class="upload-menu-popup__label">{{ t('gallery.typeVideo', '视频') }}</span>
      </button>

      <button type="button" class="upload-menu-popup__item" @click="handleSelect('text')">
        <q-icon name="description" size="22px" class="upload-menu-popup__icon" />
        <span class="upload-menu-popup__label">{{ t('gallery.typeText', '文本') }}</span>
      </button>
    </div>
  </q-menu>
</template>

<script setup lang="ts">
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

function handleSelect(type: string) {
  // TODO: Implement actual upload/selection logic for the selected type
  console.debug('Selected type:', type)
}
</script>

<style lang="scss">
/* Target the QMenu portal wrapper to allow the caret to overflow and prevent double backgrounds */
.upload-menu-popup {
  background: transparent !important;
  box-shadow: none !important;
  border: none !important;
  overflow: visible !important;
}
</style>

<style lang="scss" scoped>
.upload-menu-popup__container {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 8px;
  width: 140px;
  padding: 8px;
  background: rgba(16, 18, 28, 0.95);
  border-radius: 16px;
  border: 1px solid rgba(0, 240, 255, 0.15);
  backdrop-filter: var(--imago-blur-panel, blur(24px));
  -webkit-backdrop-filter: var(--imago-blur-panel, blur(24px));
  box-shadow: 
    0 16px 40px rgba(0, 0, 0, 0.6),
    inset 0 0 20px rgba(0, 240, 255, 0.05);
  
  /* The downward pointing caret/arrow */
  &::after {
    content: '';
    position: absolute;
    bottom: -6px;
    left: 50%;
    transform: translateX(-50%) rotate(45deg);
    width: 12px;
    height: 12px;
    background: rgba(16, 18, 28, 0.95);
    border-right: 1px solid rgba(0, 240, 255, 0.2);
    border-bottom: 1px solid rgba(0, 240, 255, 0.2);
    box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.3);
    z-index: -1;
  }
}

.upload-menu-popup__item {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 12px 16px;
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 10px;
  color: var(--imago-text-primary, #fff);
  cursor: pointer;
  transition: all var(--imago-ease-fast, 0.2s ease);
  outline: none;

  &:hover,
  &:focus-visible {
    background: rgba(0, 240, 255, 0.06);
    border-color: rgba(0, 240, 255, 0.5);
    box-shadow: 
      0 0 16px rgba(0, 240, 255, 0.15),
      inset 0 0 12px rgba(0, 240, 255, 0.1);

    .upload-menu-popup__icon {
      color: var(--imago-cyan-bright, #00f0ff);
      filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.5));
    }
  }
}

.upload-menu-popup__icon {
  color: #8b99ff; /* Soft purplish blue */
  transition: all var(--imago-ease-fast, 0.2s ease);
}

.upload-menu-popup__label {
  font-size: 15px;
  font-weight: 500;
  letter-spacing: 0.5px;
}
</style>
