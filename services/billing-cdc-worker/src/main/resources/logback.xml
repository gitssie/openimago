<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Quiet down the noisy Debezium/Kafka internals in production -->
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="io.debezium" level="INFO"/>
    <logger name="io.debezium.connector.postgresql" level="INFO"/>
    <logger name="io.debezium.embedded" level="INFO"/>
    <logger name="io.debezium.storage" level="INFO"/>

    <!-- Application logger -->
    <logger name="com.openimago.billingcdc" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
